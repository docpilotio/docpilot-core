# RFC-0062 Starting Prompt

Use the RFC-0061 completed worktree as the baseline and work with Codex directly,
without DocPilot MCP.

Preserve DIR 0.4, Snapshot format 1/2 compatibility, RFC-0052 Artifact identities,
Profile identities, Evolution Report format 1, and legacy rendering.

Extend deterministic navigation analysis only where source Evidence proves identity.
Prioritize function-reference destination content, structured nested-graph ownership,
and navigation argument Evidence. Do not infer runtime back-stack behavior, business
meaning, or Feature boundaries from names. Ambiguous targets remain deterministic
`UnresolvedItem` records.

Implement production code, targeted tests, full Gradle verification, real-project
validation, documentation synchronization, and a completion handoff. Do not commit,
push, merge, tag, or release.
