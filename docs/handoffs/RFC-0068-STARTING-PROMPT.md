# RFC-0068 Starting Prompt — Official Documentation Generation CLI

RFC-0068 provides the official `docpilot generate docs` product entry point. It composes the existing deterministic Specification, Profile, Artifact Catalog, selective renderer, ownership, and Snapshot contracts without moving canonical decisions into the CLI.

The previous “Contract Documentation Impact and Coverage” text was written before canonical RFC renumbering and is retained in Git history.
