# RFC-0068 Starting Prompt — Contract Documentation Impact and Coverage

Begin from RFC-0067's Profile-aware Contract Catalog/Detail artifacts. The canonical input remains persisted DIR 0.5 Contracts. Preserve Contract Stable IDs, deterministic paths/content, Snapshot format 3, Review Bundle format 1, Evolution format 1, Feature identities, and `NO_CHANGES` behavior.

RFC-0068 should formalize document-impact coverage across Contract changes and directly affected documentation consumers, using existing Specification Diff and Artifact Plan semantics. Do not rescan source during rendering, infer Contract meaning, add AI narrative, redesign Evolution, or implement RFC-0069 traceability/RFC-0070 reconciliation CLI concerns.

Carry forward the validation limitation: `architecture-samples` proves PUBLIC_API and CALLBACK only; the other seven roles require fixtures until an Evidence-backed real-project corpus is available. Product Validation and PV-009 remain unchanged.
