# RFC-0068 Main Planning Update

RFC-0068 is implemented on `codex/rfc-0068-documentation-generation-cli`. The official command composes existing deterministic Core contracts and keeps project input read-only. RFC-0069 is next and owns Documentation Bundle and Manifest.
