# DocPilot Roadmap

## Current Milestone

### v0.5 MVP / POC — RFC-0001 through RFC-0044

Status: RFC-0044 Relationship Semantics is completed and independently verified. Technical runtime gates, specification incremental execution, snapshot persistence, AI incremental generation, documentation diff/review, and deterministic relationship semantics have evidence. Artifact-version policy and Release Candidate approval remain pending.

Delivered baseline:

- source scanning
- knowledge construction
- DIR 0.3 specification building and deterministic Markdown rendering
- stable-ID specification incremental planning
- specification snapshot persistence and CLI workflow
- provider-independent AI incremental patch generation
- deterministic documentation diff and complete-review-before-merge
- deterministic relationship endpoint and dependency semantics
- prompt-package generation
- AI Provider SPI
- verified Ollama architecture generation

## Next

The next RFC is unconfirmed until Main Planning approval. Core Architecture Analysis and RFC Candidate Backlog preparation may proceed under the MCP-governed Codex methodology without implying approval of any candidate.

MCP is `MAINTENANCE_ONLY` and temporarily retained for Core delivery governance. Direct Codex-only transition is not approved.

## Release Validation Policy

Each release should retain a versioned snapshot containing build, test, CLI, provider, and error-handling evidence. OpenAI runtime validation is not implied unless explicitly included in release scope.
